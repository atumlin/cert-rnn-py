# Public API

Stub. Lands with each module:

- Phase 1: `cert_rnn.Zono`, `cert_rnn.transformers.*`, `cert_rnn.lstm.*`,
  `cert_rnn.rnn.*`, `cert_rnn.audit.lp_feasible`.
- Phase 2: `cert_rnn.from_torch.lstm_to_model_dict`, `rnn_to_model_dict`.
- Phase 3: `cert_rnn.verify.bisect_epsilon`, `spec_a_*`, `spec_c_*`,
  `reach_set`.
